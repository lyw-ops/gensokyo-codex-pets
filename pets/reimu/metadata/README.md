# Reimu metadata

`pet.v2.example.json` records the intended local v2 manifest shape. It is not an installable package and must not be copied into the Codex pet directory until a validated `spritesheet.webp` exists.

Before packaging, recheck the current app contract, copy the approved example to `pet.json`, confirm the stable ID and description, and ensure `spriteVersionNumber` remains `2` for the 1536×2288 atlas.
